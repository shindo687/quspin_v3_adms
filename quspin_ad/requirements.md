# Upstream provenance

- Source: https://github.com/QuSpin/QuSpin.git (official QuSpin repository)
- Commit: `5bf9e5b266e6d8b70e5cf5973c7c7d59d62e412f`
- Clone time (UTC): 2026-09-03T16:27:23Z (recorded at snapshot import; clone
  completed immediately before the snapshot was copied)
- Import method: `git clone https://github.com/QuSpin/QuSpin.git upstream`
- The clone's `.git` directory was deleted before the single snapshot commit
  (`c1d19ff`, “Import QuSpin upstream snapshot 5bf9e5b”).
- `upstream/` is read-only for this project.  All new code is under
  `quspin_ad/` and tests under `tests/`.
